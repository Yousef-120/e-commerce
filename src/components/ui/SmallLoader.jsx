
const SmallLoader = ({className}) => {
  return (
    <div className={`w-10 h-10 border-2 border-gray-600 border-t-white rounded-full animate-spin ${className}`} />
  );
};

export default SmallLoader;
